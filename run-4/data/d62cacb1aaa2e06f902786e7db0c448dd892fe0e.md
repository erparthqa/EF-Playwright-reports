# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: regression/EF-002-installation-cases.spec.ts >> EF-002 installation cases (data-driven) >> CONSUMER-DUAL-REMAINING — Consumer (dual): vehicle ALREADY transferred to the customer (dual plan) with its primary installed -> install ONLY the remaining (backup) tracker, NO re-transfer -> verify on Customer List @EF-002 @installation
- Location: tests/regression/EF-002-installation-cases.spec.ts:295:9

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('row', { name: /raymond.green1@example.com/i }).first()
Expected: visible
Timeout: 15000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 15000ms
  - waiting for getByRole('row', { name: /raymond.green1@example.com/i }).first()

```

```yaml
- banner:
  - button
  - link "Organization Logo":
    - /url: /v2
    - img "Organization Logo"
  - textbox "Search VIN, Stock, Device ..."
  - heading "Current Time (PST)" [level=6]
  - heading "13:38:05" [level=4]
  - text: IT
- main:
  - heading "Customer List" [level=3]
  - textbox "Search here...": raymond.green1@example.com
  - button "Clear search"
  - button "Show Filters"
  - button
  - button "Export"
  - button "Actions"
  - button "Dealership James Patrick"
  - button "Account"
  - button "Customer Status Active"
  - table:
    - rowgroup:
      - row "Customer Name Contact Details":
        - columnheader
        - columnheader "Customer Name":
          - text: Customer Name
          - img
        - columnheader "Contact Details":
          - text: Contact Details
          - img
    - rowgroup:
      - row "Raymond Greenmjeyin raymond.green1mjeyin@example.com -":
        - cell
        - cell "Raymond Greenmjeyin"
        - cell "raymond.green1mjeyin@example.com -"
      - row:
        - cell
  - text: Show
  - combobox:
    - option "15"
    - option "25" [selected]
    - option "50"
    - option "100"
  - text: entries
  - button [disabled]
  - button "1"
  - button [disabled]
  - text: 1 - 1 of 1 entries
- region "Notifications alt+T"
```

# Test source

```ts
  47  |     this.heading = this.page.getByRole('heading', { name: /^Customer List$/i });
  48  |     this.searchInput = this.page.getByPlaceholder(/Search here/i).first();
  49  |     this.detailsDialog = this.page.getByRole('dialog', { name: /Add\/Edit Customer Details/i });
  50  |     this.detailsHeading = this.page.getByRole('heading', { name: /Add\/Edit Customer Details/i });
  51  |     this.vehiclesTrackersTab = this.page.getByRole('tab', { name: /Vehicles\s*\/\s*Trackers/i });
  52  |   }
  53  | 
  54  |   /**
  55  |    * Clear anything that intercepts clicks before interacting: wait out the app's
  56  |    * "Downloading update…" modal (it can take a while to finish downloading) and
  57  |    * Escape/close any first-visit intro.js tour. Generous timeout so a slow
  58  |    * update download doesn't fail the click.
  59  |    */
  60  |   private async clearOverlays(): Promise<void> {
  61  |     await waitForNoUpdateModal(this.page, 120_000);
  62  |     await dismissProductTour(this.page);
  63  |     await this.dismissReportWelcome();
  64  |   }
  65  | 
  66  |   /**
  67  |    * Dismiss the "Hii, Welcome to new report design 👋" onboarding tour dialog the
  68  |    * report pages (incl. Customer List) show on first visit. It's a role=dialog
  69  |    * with Back/Next and an X, and it sits ON TOP of the results table — blocking
  70  |    * the search/row clicks (consumer verification then times out). Best-effort:
  71  |    * click a labelled close/skip, else the icon (X) button that isn't Back/Next,
  72  |    * else press Escape.
  73  |    */
  74  |   private async dismissReportWelcome(): Promise<void> {
  75  |     const dlg = this.page
  76  |       .getByRole('dialog')
  77  |       .filter({ hasText: /welcome to new report design|new report design/i })
  78  |       .first();
  79  |     for (let i = 0; i < 3; i++) {
  80  |       if (!(await dlg.isVisible({ timeout: 800 }).catch(() => false))) return;
  81  |       const labelled = dlg
  82  |         .getByRole('button', { name: /close|skip|got it|done|finish|dismiss/i })
  83  |         .first();
  84  |       if (await labelled.isVisible({ timeout: 400 }).catch(() => false)) {
  85  |         await labelled.click().catch(() => {});
  86  |       } else {
  87  |         // The close control is an icon-only button (no text label) — click the
  88  |         // dialog button that isn't Back/Next; fall back to Escape.
  89  |         const btns = dlg.getByRole('button');
  90  |         const n = await btns.count().catch(() => 0);
  91  |         let clicked = false;
  92  |         for (let b = 0; b < n; b++) {
  93  |           const txt = (await btns.nth(b).innerText().catch(() => '')).trim().toLowerCase();
  94  |           if (txt === 'back' || txt === 'next') continue;
  95  |           await btns.nth(b).click().catch(() => {});
  96  |           clicked = true;
  97  |           break;
  98  |         }
  99  |         if (!clicked) await this.page.keyboard.press('Escape').catch(() => {});
  100 |       }
  101 |       await this.page.waitForTimeout(500);
  102 |     }
  103 |   }
  104 | 
  105 |   /**
  106 |    * Open the Customer List report: avatar → "Organization and Device Lists" →
  107 |    * "Customer List". Idempotent-ish: if already on the report, does nothing.
  108 |    */
  109 |   async open(): Promise<void> {
  110 |     if (await this.heading.isVisible({ timeout: 1_500 }).catch(() => false)) return;
  111 | 
  112 |     await waitForNoUpdateModal(this.page);
  113 |     for (let attempt = 1; attempt <= 3; attempt++) {
  114 |       await this.avatarTrigger.click().catch(() => {});
  115 |       if (await this.orgListsMenuItem.isVisible({ timeout: 4_000 }).catch(() => false)) break;
  116 |       await this.page.waitForTimeout(500);
  117 |     }
  118 |     await this.orgListsMenuItem.click();
  119 |     await this.customerListMenuItem.waitFor({ state: 'visible', timeout: 10_000 });
  120 |     await this.customerListMenuItem.click();
  121 |     await this.page.waitForURL(/\/report\/CustomerList/i, { timeout: 20_000 }).catch(() => {});
  122 |     await this.heading.waitFor({ state: 'visible', timeout: 20_000 });
  123 |     // The report can pop the app update modal and/or a first-visit intro.js tour,
  124 |     // both of which intercept clicks — clear them before interacting.
  125 |     await this.clearOverlays();
  126 |   }
  127 | 
  128 |   /** Type an email (or name) into the "Search here..." box and let it filter. */
  129 |   async searchCustomer(query: string): Promise<void> {
  130 |     await this.clearOverlays();
  131 |     await this.searchInput.click({ timeout: 45_000 });
  132 |     // React-controlled input can drop characters — type, verify, retry.
  133 |     for (let attempt = 0; attempt < 4; attempt++) {
  134 |       await this.searchInput.fill('');
  135 |       await this.page.waitForTimeout(150);
  136 |       await this.searchInput.pressSequentially(query, { delay: 30 });
  137 |       await this.page.waitForTimeout(400);
  138 |       if ((await this.searchInput.inputValue().catch(() => '')) === query) break;
  139 |     }
  140 |     await this.page.waitForTimeout(2_500); // debounced server-side filter
  141 |   }
  142 | 
  143 |   /** Assert the customer row is present (matched by email or name). */
  144 |   async verifyCustomerFound(match: string): Promise<void> {
  145 |     await expect(
  146 |       this.page.getByRole('row', { name: new RegExp(match, 'i') }).first()
> 147 |     ).toBeVisible({ timeout: 15_000 });
      |       ^ Error: expect(locator).toBeVisible() failed
  148 |   }
  149 | 
  150 |   /** Open the customer's detail dialog by clicking their Customer Name cell. */
  151 |   async openCustomer(name: string): Promise<void> {
  152 |     await this.clearOverlays();
  153 |     const cell = this.page.getByRole('cell', { name: new RegExp(name, 'i') }).first();
  154 |     await cell.waitFor({ state: 'visible', timeout: 15_000 });
  155 |     await cell.click({ timeout: 45_000 });
  156 |     // The dialog's role wrapper is a zero-box positioning element (reports
  157 |     // "hidden"); wait for its visible heading instead.
  158 |     await this.detailsHeading.waitFor({ state: 'visible', timeout: 20_000 });
  159 |   }
  160 | 
  161 |   /** Switch to the customer's "Vehicles/Trackers" tab. */
  162 |   async openVehiclesTrackers(): Promise<void> {
  163 |     await this.clearOverlays();
  164 |     await this.vehiclesTrackersTab.click({ timeout: 45_000 });
  165 |     // The tab renders a table with a "Tracker/Tag ID" column header.
  166 |     await expect(
  167 |       this.detailsDialog.getByText(/Tracker\/Tag ID/i).first()
  168 |     ).toBeVisible({ timeout: 15_000 });
  169 |   }
  170 | 
  171 |   /**
  172 |    * On the open customer's "Vehicles/Trackers" tab, assert the tracker/tag id is
  173 |    * listed (and, when provided, the vehicle text too).
  174 |    */
  175 |   async verifyTrackerListed(deviceId: string, vehicle?: string): Promise<void> {
  176 |     await expect(
  177 |       this.detailsDialog.getByText(deviceId, { exact: false }).first()
  178 |     ).toBeVisible({ timeout: 15_000 });
  179 |     if (vehicle) {
  180 |       await expect(
  181 |         this.detailsDialog.getByText(vehicle, { exact: false }).first()
  182 |       ).toBeVisible({ timeout: 15_000 });
  183 |     }
  184 |   }
  185 | 
  186 |   /**
  187 |    * Full consumer verification: open the report, search by email, confirm the
  188 |    * customer, open them, and assert the device(s) show on Vehicles/Trackers.
  189 |    * `extraDeviceIds` lets a dual install confirm BOTH trackers (primary+backup).
  190 |    */
  191 |   async verifyCustomerTracker(opts: {
  192 |     email: string;
  193 |     name: string;
  194 |     deviceId: string;
  195 |     extraDeviceIds?: string[];
  196 |     vehicle?: string;
  197 |   }): Promise<void> {
  198 |     await this.open();
  199 |     await this.searchCustomer(opts.email);
  200 |     await this.verifyCustomerFound(opts.email);
  201 |     await this.openCustomer(opts.name);
  202 |     await this.openVehiclesTrackers();
  203 |     await this.verifyTrackerListed(opts.deviceId, opts.vehicle);
  204 |     for (const id of opts.extraDeviceIds ?? []) {
  205 |       await this.verifyTrackerListed(id);
  206 |     }
  207 |   }
  208 | 
  209 |   /** Close the customer details dialog (best-effort) and return to a clean map. */
  210 |   async close(): Promise<void> {
  211 |     for (let i = 0; i < 3 && (await this.detailsDialog.isVisible().catch(() => false)); i++) {
  212 |       await this.page.keyboard.press('Escape').catch(() => {});
  213 |       await this.page.waitForTimeout(400);
  214 |     }
  215 |     await this.page.goto(`${env.appUrl}/accounts/`, { waitUntil: 'domcontentloaded' }).catch(() => {});
  216 |   }
  217 | }
  218 | 
```